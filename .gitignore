_site
.bundle
# Agent worktrees are a full second checkout of this repo; never commit one.
# Left deliberately narrow so .claude/settings.json and friends can still be
# tracked if wanted.
.claude/worktrees/
.DS_store
.idea
.jekyll-cache
.jekyll-metadata
.ruby-version
.sass-cache
.tweet-cache
.venv
assets/libs/
assets/rendercv/rendercv_output/*.typ
Gemfile.lock
node_modules/
vendor